fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
SQLAlchemy==2.0.36
python-dotenv==1.0.1

# Optional providers — the app runs without them (template fallback always works)
anthropic==0.42.0
openai==1.59.6

# Optional infra drop-ins (documented in README; not required for local run)
# psycopg[binary]==3.2.3     # Postgres
# pymongo==4.10.1            # Mongo span store
# redis==5.2.1              # Redis rate limiter

# Dev / test
pytest==8.3.4
httpx==0.28.1
pyjwt[crypto]>=2.9
